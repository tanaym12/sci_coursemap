import { GraphNode } from "../../graph";
import { Lane } from "./index";
/** the effective children for grid layouts */
export declare function gridChildren(node: GraphNode): Set<GraphNode>;
/**
 * Verify that nodes were assigned valid lanes
 */
export declare function verifyLanes(ordered: GraphNode[], lane: Lane): number;
